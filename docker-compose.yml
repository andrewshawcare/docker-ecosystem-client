version: "2"
volumes:
  database-data:
services:
  docker-ecosystem-client:
    image: ${REGISTRYHOST}/docker-ecosystem-client:${TAG}
    build: .
    ports:
      - '80'
    links:
      - java-service
      - node-service
  java-service:
    image: ${REGISTRYHOST}/docker-ecosystem-java-service
    links:
      - database
    depends_on:
      - migration
  node-service:
    image: ${REGISTRYHOST}/docker-ecosystem-node-service
    links:
      - database
    depends_on:
      - migration
  migration:
    image: ${REGISTRYHOST}/docker-ecosystem-migration
    links:
      - database
  database:
    image: postgres:9.5.1
    volumes:
      - database-data:/var/lib/postgres/data
